Command Used to spark submit the job
======================================

# spark-submit --class edu.pa.Convolution --master yarn-cluster --executor-memory 1G --num-executors 15  --executor-cores 2 --conf spark.yarn.executor.memoryOverhead=784 --driver-memory 14G /home/gupt2734/NeuroHadoopScala-0.0.1-SNAPSHOT.jar

===============================================================================================================

# The Output will be generated in hdfs under folder name "resultspark"

# The intermediate Rat records will be stored on hdfs under folder "rats" in parquet data format.

# P.S. Remove resultspark and rats folder from hdfs before each run.